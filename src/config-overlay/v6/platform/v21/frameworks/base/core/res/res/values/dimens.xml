<resources>
    <add-resource type="dimen" name="glowpadview_glow_radius" />
    <add-resource type="dimen" name="glowpadview_inner_radius" />
    <add-resource type="dimen" name="system_bar_height" />

    <!-- Default glow radius for GlowPadView -->
    <dimen name="glowpadview_glow_radius">75dip</dimen>
    <!-- Default distance from each snap target that GlowPadView considers a "hit" -->
    <dimen name="glowpadview_inner_radius">15dip</dimen>
    <!-- Height of the system bar (combined status & navigation); used by
         SystemUI internally, not respected by the window manager. -->
    <dimen name="system_bar_height">48dip</dimen>

    <!-- Top padding for notifications in the standard layout. -->
    <dimen name="notification_top_pad">0dp</dimen>
    <!-- Top padding for notifications when narrow (i.e. it has 3 lines) -->
    <dimen name="notification_top_pad_narrow">0dp</dimen>
    <!-- Padding for notification icon when drawn with circle around it -->
    <dimen name="notification_large_icon_circle_padding">0dp</dimen>
</resources>
