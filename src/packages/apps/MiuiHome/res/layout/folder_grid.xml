<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/background"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/folder_content_bg" >
    <com.miui.home.launcher.DropableGridView
        android:id="@+id/folder_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:gravity="center"
        android:clipToPadding="false"
        android:paddingLeft="@dimen/folder_content_padding_left"
        android:paddingTop="@dimen/folder_content_padding_top"
        android:paddingRight="@dimen/folder_content_padding_right"
        android:paddingBottom="@dimen/folder_content_padding_bottom"
        android:cacheColorHint="#00000000"
        android:scrollbars="vertical"
        android:scrollbarStyle="insideInset"
        android:scrollbarThumbVertical="@drawable/folder_scrollbar_v"
        android:drawSelectorOnTop="true"
        android:listSelector="@drawable/grid_selector"
        android:verticalSpacing="5dip"
        android:horizontalSpacing="5dip"
        android:numColumns="@integer/config_folder_columns_count"
        android:animationCache="false"
        android:requiresFadingEdge="vertical"
        android:fadingEdgeLength="18sp" />
</FrameLayout>