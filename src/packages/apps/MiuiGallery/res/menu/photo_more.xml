<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- TODO: Need final icon assets.  Also, PopupMenu currently ignores the
         android:icon attribute anyway(!) -->
    <item android:id="@+id/apply_for"
          android:title="@string/apply_pic_for"/>
    <item android:id="@+id/show_on_map"
          android:title="@string/show_on_map" />
    <item android:id="@+id/show_details"
          android:title="@string/show_details" />
</menu>
