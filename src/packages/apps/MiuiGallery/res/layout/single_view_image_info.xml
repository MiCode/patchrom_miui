<?xml version="1.0" encoding="UTF-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="bottom"
    android:background="@drawable/single_view_menu_bg"
    android:inAnimation="@android:anim/fade_in"
    android:outAnimation="@android:anim/fade_out"
    android:gravity ="center_vertical"
>
    <ImageView android:id="@+id/image_info_tag"
        style="@style/SingleViewButton"
        android:src="@drawable/image_info_tag_n"
    />
    <ImageView android:id="@+id/play_slideshow"
        style="@style/SingleViewButton"
        android:src="@drawable/play_slideshow_n"
    />
    <TextView android:id="@+id/image_order_txt"
        android:layout_width="0dip"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:gravity="center"
        android:singleLine="true"
        android:textSize="13sp"
        android:textColor="@android:color/white"
    />
</LinearLayout>
