<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/nearby_location_change_holder"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center_vertical"
    android:orientation="vertical"
    android:visibility="gone" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:orientation="vertical" >

        <TextView
            android:id="@+id/text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/personal_location_changed"
            android:textAppearance="@style/TextAppearance.Title.Secondary.NoAntiColor" />

        <Button
            android:id="@+id/action_button"
            style="@style/Widget.Button.Retry"
            android:layout_marginTop="@dimen/common_padding"
            android:text="@string/personal_change_city_button" />
    </LinearLayout>

</LinearLayout>