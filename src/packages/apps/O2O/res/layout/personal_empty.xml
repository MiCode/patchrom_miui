<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/personal_item_empty"
    android:layout_width="match_parent"
    android:layout_height="@dimen/no_personal_passbook_height"
    android:background="@drawable/detail_content_border"
    android:gravity="center"
    android:visibility="gone" >

    <TextView
        android:id="@+id/no_personal_passbook"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/no_personal_passbook"
        android:textAppearance="@style/TextAppearance.Title.Secondary.NoAntiColor" />

</LinearLayout>