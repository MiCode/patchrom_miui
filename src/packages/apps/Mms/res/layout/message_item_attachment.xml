<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android">
    <ImageView
        android:id="@+id/attachment_pic"
        android:layout_width="@dimen/mms_attachment_pic_width"
        android:layout_height="@dimen/mms_attachment_pic_height"
        android:layout_gravity="top"
        android:visibility="gone"
        android:background="@drawable/message_attachment_pic" />
    <ImageView
        android:id="@+id/attachment_card"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:visibility="gone"
        android:background="@drawable/message_attachment_card" />
    <ImageView
        android:id="@+id/attachment_audio"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:visibility="gone"
        android:background="@drawable/message_attachment_audio" />
    <ImageView
        android:id="@+id/attachment_video"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:visibility="gone"
        android:background="@drawable/message_attachment_video" />
    <ImageView
        android:id="@+id/attachment_default"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:visibility="gone"
        android:background="@drawable/message_attachment_default" />
</merge>