<?xml version="1.0" encoding="utf-8"?>
<preference-headers
        xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Timeout -->
    <header
        android:id="@+id/screen_timeout_settings"
        android:title="@string/screen_timeout_title"
        android:icon="@drawable/ic_screen_timeout_settings" >
        <intent
            android:targetPackage="com.android.settings"
            android:targetClass="com.android.settings.display.ScreenTimeoutDialogActivity" />
    </header>
</preference-headers>