<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/group_detail"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent" >

        <ListView
            android:id="@android:id/list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingLeft="@dimen/group_list_padding_left"
            android:paddingRight="@dimen/group_list_padding_right"
            android:divider="@null"
            android:fadingEdge="none"
            android:scrollbarStyle="outsideOverlay" />

        <!--
          Shadow overlay over the list of group members (since we have a fake stacked
          action bar)
        -->

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="@dimen/group_detail_shadow_overlay_height"
            android:background="?android:attr/windowContentOverlay" />
    </FrameLayout>

</LinearLayout>